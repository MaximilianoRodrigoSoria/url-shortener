# Performance del build de Gradle
org.gradle.caching=true
org.gradle.parallel=true

# El configuration cache puede requerir que todos los plugins lo soporten
# (spotbugs/pmd). Activarlo y validar con 'gradlew check' antes de fijarlo:
# org.gradle.configuration-cache=true
